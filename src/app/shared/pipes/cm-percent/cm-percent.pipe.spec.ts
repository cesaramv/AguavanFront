import { CmPercentPipe } from './cm-percent.pipe';

describe('CmPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new CmPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
